#include <string>
#include <iostream>

using namespace std;

#ifndef VISUAL_HPP
#define VISUAL_HPP

namespace Visual {

void Menu();
void printShape(int r);
void ErrorHandling(int num_particles, int dimensions, int max_iter);

}
#endif